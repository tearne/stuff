#!/bin/bash

export DEBIAN_FRONTEND=noninteractive

sudo apt-get update \
  && sudo apt-get install -y libxss1 \
  && sudo add-apt-repository -y ppa:webupd8team/atom \
  && sudo apt-get update \
  && sudo apt-get install -y --no-install-recommends atom
